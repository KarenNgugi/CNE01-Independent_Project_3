apiVersion: v1
kind: Service
metadata:
  namespace: grades-tracker-namespace
  name: headless-svc
  labels:
    app: grades-tracker-app
    tier: database
spec:
  selector:
    app: grades-tracker-app
    tier: database
  clusterIP: None
  ports:
  - name: db-headless
    port: 5432

