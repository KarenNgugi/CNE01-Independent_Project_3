apiVersion: v1
kind: Service
metadata:
  namespace: grades-tracker-namespace
  name: frontend-svc
spec:
  type: NodePort
  selector:
    tier: frontend
  ports:
  - name: frontend-service
    protocol: TCP
    port: 80
    targetPort: frontend-port
    nodePort: 30090
